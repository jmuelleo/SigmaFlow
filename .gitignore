# SigmaDock is a local, read-only reference copy of the original repo
# (github.com/alvaroprat97/sigmadock) - not part of the SigmaFlow deliverable.
/SigmaDock/

# Python
__pycache__/
*.pyc

# Generated sampling outputs, checkpoints, and benchmark results (never
# committed per CLAUDE.md §9 - regenerable from a checkpoint + sample.py).
# Matches at any depth so this also covers SigmaFlow_MinimalChange/,
# SigmaFlow_Adapted/, and SigmaFlow_Variants/<name>/, not just
# SigmaFlow_Development/.
**/experiments/
# ABER: conf/experiments/ sind KEINE Ausgaben, sondern die
# DATENSATZDEFINITIONEN (pdbbind-general.yaml, posebusters.yaml, ...).
# Sie wurden von der Regel darueber versehentlich mitverschluckt, waren
# deshalb nie versioniert und fehlten im ARC-Checkout. Der Throughput-Sweep
# 8583394/8583395 ist daran gescheitert:
#   ValueError: Experiment config not found: .../conf/experiments/pdbbind-core.yaml
# Die 72h-Laeufe waeren mit demselben Fehler nach Sekunden gestorben.
# Verzeichnis UND Inhalt muessen einzeln zurueckgeholt werden -- git steigt
# sonst gar nicht erst in ein ausgeschlossenes Verzeichnis hinab.
!**/conf/experiments/
!**/conf/experiments/**
**/sampling_output*/
**/posebusters_results/
**/pymol_scripts/
**/Screenshots*/
**/rmsd_summary_all_runs.csv
**/cache/

# Local working copies used for the SigmaDock-vs-SigmaFlow bond-length
# audit (2026-08-05): frozen baseline snapshot, adaptation sandbox, and
# collector for tried-and-compared loss/architecture variants. Source-only
# (see gitignore rules above for what's excluded within them); not part of
# the tracked SigmaFlow_Development deliverable.
/SigmaFlow_MinimalChange/
/SigmaFlow_Adapted/

# --- 2026-08-13: Reorganisation ---
# Grosse Artefakte nie ins Repo
*.tar.gz
# Varianten: QUELLCODE versionieren, Ausgaben nicht.
# (**/experiments/ und **/sampling_output*/ oben greifen bereits)
!/SigmaFlow_Variants/
/SigmaFlow_Variants/**/pymol_cases/
/SigmaFlow_Variants/**/true_ligands/
/SigmaFlow_Variants/**/*_pred/
/SigmaFlow_Variants/**/stepsweep/
/SigmaFlow_Variants/**/seeds10_*/

# Erzeugt aus einer Messung, nie von Hand -- gehoert nicht in die Historie
arc/final_horizon.env

# --- Eingefroren 2026-08-18 -------------------------------------------------
# Synthetische Visualisierungs-Demo. Erzeugt von visualization/build_case.py
# aus KONSTRUIERTEN Trajektorien, nicht aus echtem Sampling. Sie darf nie in
# einen Ergebnisbericht wandern, also gehoert sie auch nicht in die Historie.
visualisations/
# LaTeX-Bauartefakte
*.aux
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.Rhistory

# Regenerierbar aus fragments_vs_performance.csv via visualization/plot_fragments.py
*_fragment_distribution.png
*_fragments_vs_size.png
*_fragments_vs_performance.png

# Lokale PyMOL-Arbeitsordner (Kopien von ARC-Daten, regenerierbar)
vis_*/

# Der Thesis-Ablageordner ist ein bewusstes Ergebnis, kein Nebenprodukt --
# die PNG-Regeln oben duerfen ihn nicht mitnehmen.
!Thesis Visualisierungen/**
